export enum RequestTypeEnum {
  code = 'code',
  geocode = 'geocode',
  elevate = 'elevate',
  autocode = 'autocode',
  tripe = 'tripe',
  distance_time = 'distance_time'
}

export default RequestTypeEnum;
